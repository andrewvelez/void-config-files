#! /bin/bash
#  $HOME/.config/bash/bash_profile
#  bash_profile is called for all text, gui shells but not all bash invocations like init scripts
#  by Andrew Velez

[[ -r "$HOME/.bashrc" ]] && . "$HOME/.bashrc"

# sudo sv status /etc/sv/dinitd > /dev/null && /home/andrew/.local/bin/dinitsys start dinit-user-d